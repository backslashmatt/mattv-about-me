rules_version = '2';

// This site is a static Firebase Hosting deployment and does not read from or
// write to Firestore. Deny all client access; add scoped rules here if that
// ever changes.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
